{
  "private": true,
  "scripts": {
    "dev": "turbo run dev --parallel --continue",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "changeset": "changeset",
    "version-pkg": "changeset version",
    "publish": "changeset publish"
  },
  "author": "Baiyusoup",
  "devDependencies": {
    "@baiyusoup/eslint-config-react": "^0.1.0",
    "@baiyusoup/eslint-config-vanilla": "^0.1.0",
    "@changesets/cli": "^2.22.0",
    "eslint": "^8.17.0",
    "turbo": "^1.2.16",
    "typescript": "^4.7.3"
  }
}
